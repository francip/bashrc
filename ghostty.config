# Offline config reference docs
#   ghostty +show-config --default --docs

# Default background color (see docs above for all options)
background = #000000

# Brighter ANSI palette (matches popular "Tomorrow Night Bright" theme)
theme = "Tomorrow Night Bright"

# Use bright variants for bold text
bold-color = bright
